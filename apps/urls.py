"""
Centralized URL configuration for all domain apps
"""
from django.urls import path, include

app_name = 'apps'

urlpatterns = [
    # Domain-specific URL patterns can be included here if needed
    # For now, all URLs are in core/urls.py for backward compatibility
]
